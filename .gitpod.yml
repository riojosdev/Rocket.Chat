# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/introduction/learn-gitpod/gitpod-yaml)
# and commit this file to your remote git repository to share the goodness with others.

# Learn more from ready-to-use templates: https://www.gitpod.io/docs/introduction/getting-started/quickstart

tasks:
  - init: sudo apt-get update && sudo apt-get dist-upgrade -y && sudo apt-get install build-essential git curl python3-minimal pkg-config && nvm install 14 && curl https://install.meteor.com/?release=$(curl -so- https://raw.githubusercontent.com/RocketChat/Rocket.Chat/develop/apps/meteor/.meteor/release | cut -d@ -f2) | sh && export PATH=$PATH:/home/gitpod/.meteor && yarn && export ROOT_URL=$(gp url 3000)
    command: yarn build && yarn dev
